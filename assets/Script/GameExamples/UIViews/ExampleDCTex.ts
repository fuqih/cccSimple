import { BaseUIKeeperView } from "../../GameBaseUIViews/BaseUIKeeperView";

const { ccclass, property } = cc._decorator;

/**
 * 减少要渲染的物体数量,虚拟列表技巧
 * 支持合批，静态合图，动态合图，文字渲染模式选择等。节点层级动态调整(辅助前面的合图)
 */
@ccclass
export default class ExampleDCTex extends BaseUIKeeperView {

}